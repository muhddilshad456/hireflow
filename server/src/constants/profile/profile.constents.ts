export enum ProfileArrayField {
  EDUCATION = "education",
  EXPERIENCE = "experience",
  COVER_LETTERS = "coverLetters",
  RESUMES = "resumes",
}

export const ALLOWED_PROFILE_ARRAY_FIELDS = Object.values(ProfileArrayField);
